import type { Metadata } from "next";
import { Outfit } from "next/font/google";
import "./globals.css";

const outfit = Outfit({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "AIdeas Analyzer - AWS Builder Competition Leaderboard & Analytics",
  description: "Live leaderboard and analytics for the AWS 10,000 AIdeas Builder Competition. Track rankings, velocity, forecasts, and competition insights.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={outfit.className}>{children}</body>
    </html>
  );
}
